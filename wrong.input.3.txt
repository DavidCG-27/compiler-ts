
let i:int;
let f:number;

let a:[
  let a:int;
];

function main(): void {
  log i && f; 			// Error 1. La operación && no es soportada por los tipos entero y number. (solo por entero). El tipo de && es ErrorType
  log a >= 3; 			// Error 2. El tipo registro no soporta la operación de comparación. ErrorType
  while (f)     		// Error 3. El tipo number no se puede usar como condición. El ErrorType generado no se le asigna a nadie.
    if (f)      		// Error 4. El tipo number no se puede usar como condición. El ErrorType generado no se le asigna a nadie.
      log !f;   		// Error 5. El tipo number no soporta el operador lógico unario. El ErrorType generado no se le asigna a nadie. El tipo de !f es ErrorType.
}