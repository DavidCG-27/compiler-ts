let date: [ 
 let day:int;
 let month:int;
 let day:number;		// Error 1 error de campos repetidos
];

function main(): void {
  input date.year;	// Error 2 error de tipo (no existe year). Si existiera year, sería un error de nombre. Debemos de decir que "la operación . (punto) no es soportada por 'date' con el campo 'year'". Sería ErrorType
}

